server:
  port: 8080

spring:
  application:
    name: persian-date-api

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
  api-docs:
    path: /v1/api-docs
  packages-to-scan: com.tiddev.persian_date_util.controller
  paths-to-match: /api/**
